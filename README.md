# image-preview
front end image preview
### how to use it:

``` javascript
//just
new ImagePreview({
  selector:``
})
or
new ImagePreview({
  curImg:'imgsrc',
  imgs:[
    'imgsrc',
    'imgsrc',
    'imgsrc'
  ]
})
```
